# --- Appwrite (public: sent to the browser) ---
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_project_id

# --- Appwrite (server-only: NEVER expose) ---
# API key needs scopes: sessions.write, databases.read, databases.write,
# documents.read, documents.write
APPWRITE_API_KEY=your_server_api_key

# --- Database / collection IDs (match appwrite.json) ---
APPWRITE_DATABASE_ID=leetcode
APPWRITE_PHASES_COLLECTION_ID=phases
APPWRITE_PROBLEMS_COLLECTION_ID=problems
APPWRITE_TOKENS_COLLECTION_ID=mcp_tokens

# --- MCP OAuth 2.1 (for web LLM connectors like ChatGPT / Claude.ai) ---
# Secret used to sign OAuth authorization codes + access tokens. Use a long
# random string. If unset, the Appwrite API key is used as a fallback.
MCP_OAUTH_SECRET=change_me_to_a_long_random_string
# Public base URL of the deployment (used in OAuth discovery metadata). Leave
# unset to auto-detect from request/proxy headers.
# NEXT_PUBLIC_APP_URL=https://your-domain
